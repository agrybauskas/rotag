#!/usr/bin/perl

use strict;
use warnings;

use BondParameters qw( rotatable_bonds );
use ForceField::Parameters;

#
# Unit test: rotatable_bonds()
# Example: ./rotatable_bonds structure.dump
#

my ( $pdbx_dump_file ) = @ARGV;

my $atom_site;
open( my $fh, "<", $pdbx_dump_file );
while( <$fh> ) {
    $atom_site .= "$_" ;
}
close( $fh );
$atom_site = eval( $atom_site );

my $parameters = Parameters->new();
rotatable_bonds( $parameters, $atom_site );

for my $atom_id ( sort { $a cmp $b }
                  grep { defined $atom_site->{$_}{'rotatable_bonds'} }
                  keys %{ $atom_site } ) {
    my $rotatable_bonds = $atom_site->{$atom_site}{'rotatable_bonds'};
    print( "$atom_site->{$atom_id}{\"label_atom_id\"}: "  );
    for my $angle ( sort { $rotatable_bonds->{$atom_id}{$a}{'order'} <=>
                           $rotatable_bonds->{$atom_id}{$b}{'order'} }
                    keys %{ $rotatable_bonds->{$atom_id} } ) {
        my $first_atom_id = $rotatable_bonds->{$atom_id}{$angle}{'atom_ids'}[1];
        my $second_atom_id = $rotatable_bonds->{$atom_id}{$angle}{'atom_ids'}[2];
        print( "$atom_site->{$first_atom_id}{\"label_atom_id\"}," .
               "$atom_site->{$second_atom_id}{\"label_atom_id\"} " );
    }
    print( "\n" );
}
