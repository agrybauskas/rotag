package Constants;

use strict;
use warnings;

use Exporter qw( import );
our @EXPORT_OK = qw( $EPSILON
                     $PI
                     $SIG_FIGS );

our $EPSILON = @EPSILON@;

our $PI = @PI@;

our $SIG_FIGS = '%.3f';

1;
